services:
  activityreporter:
    build: .
    image: activityreporter:latest
    container_name: activityreporter
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"

  kafka:
    image: apache/kafka:3.8.0
    container_name: kafka
    restart: unless-stopped
    ports:
      - "9094:9094" # host access, e.g. for local producers/consumers
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093,PLAINTEXT_HOST://0.0.0.0:9094
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1

  timescaledb:
    image: timescale/timescaledb:latest-pg16
    container_name: timescaledb
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: activityreporter
      POSTGRES_USER: activityreporter
      POSTGRES_PASSWORD: activityreporter
    volumes:
      - timescaledb-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro

  collector:
    build:
      context: .
      dockerfile: docker/collector/Dockerfile
    image: activityreporter-collector:latest
    container_name: collector
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
      KAFKA_CONNECTION_STRING: kafka:9092
      KAFKA_METRICS_TOPIC: metrics
      COLLECTOR_ENDPOINTS: http://activityreporter:8080/metrics/
    depends_on:
      - kafka
      - activityreporter

  metrics-writer:
    build:
      context: .
      dockerfile: docker/consumer/Dockerfile
    image: activityreporter-consumer:latest
    container_name: metrics-writer
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
      KAFKA_CONNECTION_STRING: kafka:9092
      KAFKA_METRICS_TOPIC: metrics
      KAFKA_CONSUMER_GROUP_ID: timescale-writer
      TIMESCALE_HOST: timescaledb
      TIMESCALE_PORT: "5432"
      TIMESCALE_DB: activityreporter
      TIMESCALE_USER: activityreporter
      TIMESCALE_PASSWORD: activityreporter
    depends_on:
      - kafka
      - timescaledb

  jobmanager:
    build:
      context: ./docker/flink
    image: activityreporter-flink:1.19.1
    container_name: flink-jobmanager
    restart: unless-stopped
    command: jobmanager
    ports:
      - "8081:8081"
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
    volumes:
      - ./flink-jobs:/opt/flink/jobs
    depends_on:
      - kafka

  taskmanager:
    build:
      context: ./docker/flink
    image: activityreporter-flink:1.19.1
    container_name: flink-taskmanager
    restart: unless-stopped
    command: taskmanager
    environment:
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: jobmanager
        taskmanager.numberOfTaskSlots: 2
    volumes:
      - ./flink-jobs:/opt/flink/jobs
    depends_on:
      - jobmanager

volumes:
  timescaledb-data:
